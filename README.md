# HYPE WEDDING

서울·제주에서 웨딩 촬영 스튜디오를 탐색하고 문의할 수 있는 플랫폼
유저 서비스 + 관리자 시스템이 분리된 풀스택 구조

## 프로젝트 규모

- 개발 기간: 개발 중
- 참여 인원: 1명 (개인 프로젝트)

---

**유저**

- 서울 / 제주 웨딩·캐주얼 사진작가 탐색 및 상품 조회
- 조건 기반 상품 검색 (title / brand / description)
- Google OAuth 및 이메일/비밀번호 기반 회원 인증
- 온보딩 정보 수집 및 사용자 프로필 생성 (이름 / 생년월일 / 성별 / 국가 / 전화번호)
- 사진작가 패키지 상세 조회 — 작가별 Package A/B/C, 파트너(헤어메이크업/드레스/수트/부케), 촬영 디테일, 애드온
- **가격 정보는 로그인 사용자에게만 노출** — 인증된 API 별도 호출로 조회 (아래 보안 섹션 참고)
- 리뷰 작성 · 댓글/대댓글 (회원은 세션 기반, 비회원은 비밀번호로 본인 확인)
- 매거진 콘텐츠 열람 (촬영 스토리 / 가이드 콘텐츠)

**관리자**

- 서비스 운영 대시보드 (사용자 / 상품 통계 관리)
- 히어로 이미지 업로드 및 삭제 (Cloudflare R2 연동)
- 사진작가 상품 관리 (생성 / 수정 / 삭제 / 이미지 다중 업로드 / 노출 순서 제어)
- 패키지 카탈로그 관리 — 작가(Director) / 패키지(Package) / 파트너(Partner) / 애드온(Addon) / 인클루전(Inclusion) CRUD
- 리뷰 모더레이션 — 우수 리뷰 고정, 부적절한 리뷰·댓글 삭제
- 매거진 콘텐츠 관리 (작성 / 발행 / 이미지 관리)
- 지역 및 카테고리 기반 상품 분류 관리 (서울 / 제주 / 캐주얼)
- 회원 데이터 조회 및 관리
- 관리자 계정 관리 (master 권한 기반 계정 생성·삭제)

**권한 구조**

일반 유저와 관리자는 완전히 분리된 테이블과 인증 방식을 사용
일반 유저는 Google OAuth 또는 이메일/비밀번호로 로그인하고, 관리자는 별도 Admin 테이블의 ID/PW로 JWT 인증

일반 유저 쪽 `role` 필드는 `master`(매거진 작성 + 리뷰 모더레이션 권한)만 사용 — 관리자 대시보드(Admin 테이블)와는 별개의 권한 체계

## 서비스 구조

사진작가 상품 탐색 → 패키지 상세(로그인 시 가격 확인) → 북마크 저장 → 상담 문의
방문객은 리뷰/댓글로 후기를 남기고, 관리자는 어드민 패널에서 상품·카탈로그·히어로 이미지·매거진·회원·리뷰를 관리

**주요 도메인**

- `User` → Google OAuth 또는 Credentials 인증 · 온보딩 정보 포함 · `role`(master)
- `Product` ⇄ `Director`(N:M, `ProductDirector`) → `ProductImage`
- `Director` → `Package`(작가별 패키지 A/B/C)
- `Package` → `Addon` / `Inclusion` / `Partner` (N:M 조인 테이블) · 가격(`priceSNS`/`priceNoSNS`)은 인증 API로만 조회
- `Review` → `Comment`(대댓글 트리) · `Notification`(댓글 알림)
- `Magazine` → `MagazineImage` (Cascade Delete)
- `Admin` → role(`staff`/`master`) 기반 관리자 계정, User와 별개 테이블
- `Bookmark` → 유저별 상품 북마크

---

## 기술 스택

| 구분            | 사용 기술                         | 선택 이유                                                                |
| --------------- | --------------------------------- | ------------------------------------------------------------------------ |
| Frontend        | Next.js 15 (App Router), React 19 | 풀스택 단일 프로젝트로 운영 가능하고, SSR·CSR을 라우트별로 유연하게 조합 |
| Styling         | Tailwind CSS 4, shadcn/ui         | 유틸리티 클래스로 빠른 UI 구성, 재사용 가능한 컴포넌트 시스템            |
| ORM             | Prisma + PostgreSQL               | 타입 안전한 쿼리와 마이그레이션 관리                                     |
| 이미지 스토리지 | Cloudflare R2 + AWS SDK           | S3 호환 API를 그대로 사용하면서 무료 이그레스 비용, CDN 내장             |
| 이미지 처리     | sharp                             | 업로드 시점에 WebP 변환·리사이즈를 서버에서 일관되게 처리                |
| 인증 (유저)     | NextAuth v5 (JWT · Google OAuth)  | 소셜 로그인과 Credentials를 하나의 라이브러리로 통합 관리                |
| 인증 (관리자)   | jose (JWT · httpOnly Cookie)      | XSS로 토큰이 탈취되지 않도록 JS 접근 차단, 2시간 만료                    |
| 암호화          | Node.js (crypto: AES-256-GCM)     | 개인정보(이름·전화번호) 저장 시 암호화                                   |

---

## 주요 기능

**보안**

- NextAuth JWT 세션 (7일 만료) · Admin JWT 쿠키 (httpOnly · `secure` in production · 2시간 만료)
- Proxy 미들웨어로 `/admin`, `/onboarding` 접근 보호
- **가격 데이터는 페이지 props로 절대 내려주지 않음** — ISR(60초 캐싱)로 캐시되는 상품 상세 페이지에 실제 금액을 props로 심으면 로그인 여부와 무관하게 모든 방문자에게 같은 캐시가 나가버리므로, 가격은 별도 인증 API(`/api/products/[id]/pricing`)에서 세션 검증 후에만 반환
- Magic Bytes 검증 — MIME 스푸핑 방지 (JPEG/PNG/WEBP 실제 내용 확인)
- Decompression Bomb 방지 — sharp `limitInputPixels: 40M`
- AES-256-GCM으로 개인정보(이름·전화번호) 암호화 저장
- Path Traversal 방지 — 이미지 키 패턴 검증
- IP 기반 Rate Limiting (인메모리) — 로그인 10회/15분, 회원가입 5회/10분, 리뷰/댓글 작성 제한
- CSP · X-Frame-Options · X-Content-Type-Options 등 보안 헤더
- `master` role 기반 매거진 작성·리뷰 모더레이션 접근 제어 (일반 유저 role) / Admin 테이블 role 기반 어드민 접근 제어 (관리자, 별도 체계)
- bcryptjs 비밀번호 해싱 (salt rounds: 12)

---

## API 구성

- 인증 API — 회원가입, 로그인, 온보딩
- 상품 API — 목록 조회, 상세 조회, 검색, CRUD
- 가격 API — 로그인 사용자 전용 패키지 가격 조회 (`/api/products/[id]/pricing`)
- 리뷰/댓글 API — 작성·수정·삭제(회원 세션 또는 비회원 비밀번호), 대댓글, 우수 리뷰 고정(master)
- 매거진 API — 발행 콘텐츠 조회, 관리자 CRUD
- 이미지 업로드 API — Hero 이미지 / 상품 이미지 / 매거진 이미지 관리 (R2 연동)
- 관리자 API — 로그인, 계정 관리, 회원 조회, 통계, 패키지 카탈로그 CRUD

---

## 배포 환경

- **서버**: iwinv VPS (Ubuntu)
- **웹서버**: Nginx (리버스 프록시)
- **프로세스 관리**: PM2
- **DB**: PostgreSQL 18 (로컬 설치)
- **이미지**: Cloudflare R2 (S3 호환)
- **빌드**: Next.js standalone 모드
- **CI/CD**: GitHub Actions → SSH → 자동 빌드 및 배포

---

<details>
<summary><strong>개발하면서 겪은 문제들 (클릭해서 펼치기)</strong></summary>

### 01. npm 패키지 재설치 후 관리자 페이지가 아무 반응 없이 멈췄음

**발견** `node_modules` 삭제 후 재설치 시 `/admin` 접근이 브라우저 무한 대기 상태. 에러 메시지도, 터미널 로그도 없음

**원인** `proxy.ts`(미들웨어)가 `/admin` 경로 처리 시 NextAuth를 초기화하는데, `auth.config.ts`에 크리덴셜 없는 Apple OAuth 프로바이더가 등록되어 초기화 중 멈춤

**해결** Apple 프로바이더 제거, Google OAuth만 유지. 설정하지 않은 프로바이더가 아무 에러 없이 무응답을 만들 수 있다는 것을 확인

---

### 02. ISR 캐싱 전환 중, 가격 데이터가 로그인 여부와 무관하게 모든 방문자에게 노출될 뻔했음

**발견** 상품 상세 페이지를 `force-dynamic`에서 `revalidate = 60`(ISR)으로 바꾸는 과정에서, 서버 컴포넌트가 조회한 패키지 가격을 그대로 client component props로 넘기고 있었음을 발견

**원인** ISR 페이지는 첫 요청 시 렌더링된 HTML/RSC payload를 60초간 모든 방문자에게 동일하게 캐시해서 내려줌. 로그인 여부를 `!session`으로 화면에서만 가려도, 실제 가격 숫자는 이미 페이지 응답 자체에 포함되어 있어서 페이지 소스보기나 React DevTools로 누구나 확인 가능한 상태였음 — "화면에서 숨기기"와 "데이터를 안 보내기"는 다른 문제라는 걸 확인

**해결** 가격 필드는 페이지 props에서 항상 `0`으로 마스킹하고, 실제 금액은 별도 인증 API(`/api/products/[id]/pricing`)에서 세션 검증 후에만 반환하도록 분리. 페이지 자체는 ISR 캐싱을 유지하면서, 민감한 데이터만 클라이언트가 로그인 시점에 별도로 fetch하는 구조로 전환

---

### 03. 관리자 로그인 쿠키가 `secure: false`로 설정되어 있었음

**발견** 전체 API 라우트 보안 점검 중 `admin_token`(어드민 JWT) 쿠키에 `secure: false`가 하드코딩되어 있는 것을 발견

**원인** 로컬 개발(HTTP) 환경에서 쿠키가 안 잡히는 문제를 임시로 우회하며 넣어둔 값이 그대로 남아있었음. HTTPS가 아닌 경로로 요청이 오갈 경우 이 쿠키가 그대로 노출되어 관리자 권한이 탈취될 수 있는 상태

**해결** `secure: process.env.NODE_ENV === 'production'`으로 변경 — 로컬 개발 환경에서는 여전히 동작하면서, 배포 환경에서는 HTTPS 연결에서만 쿠키가 전송되도록 강제

---

### 04. 배포 후 체감 속도가 느려져서, 원인을 성능 지표로 직접 측정했음

**발견** 로컬/이전 서버에서는 느리다고 느낀 적 없던 페이지들이 Vercel 배포 후 로딩이 눈에 띄게 느려짐. "코드가 나빠졌나?"로 접근하지 않고, 먼저 무엇이 실제로 느린지부터 측정하기로 함

**원인** 거의 모든 페이지에 `export const dynamic = 'force-dynamic'`이 걸려있어서 요청마다 캐시 없이 매번 DB 왕복이 발생하고 있었음. 여기에 Vercel(서버리스 콜드 스타트) + Neon(오토 서스펜드 서버리스 Postgres) 조합의 지연 특성이 더해지면서, 상시 구동되던 이전 VPS + 로컬 Postgres 환경과는 완전히 다른 지연 프로파일이 됨 — 코드 회귀가 아니라 인프라 특성 차이였음을 확인

**해결** 세션에 의존하지 않는 페이지는 `export const revalidate = 60`(ISR)로 전환해 캐싱을 되살림. 단, 로그인 상태에 따라 달라지는 UI(북마크 버튼, 관리자 전용 버튼 등)를 페이지에서 그대로 캐싱하면 다른 유저에게 잘못된 세션 정보가 노출될 위험이 있어, 그 부분만 클라이언트 컴포넌트로 분리해 `useSession()`으로 별도 처리. 개선 효과를 `k6` 부하 테스트로 실측:

| 환경          | p95 응답시간 | 처리량     |
| ------------- | ------------ | ---------- |
| `next dev`    | 37.24s       | 2.18 req/s |
| 프로덕션 빌드 | 1.97s        | 32.8 req/s |

---

### 05. ISR 전환 후 Vercel 빌드가 `useSearchParams() should be wrapped in a suspense boundary` 에러로 실패

**발견** 위 4번 작업으로 페이지들을 ISR로 바꾸자, `force-dynamic`일 때는 문제없이 통과하던 빌드가 Vercel에서 실패

**원인** `force-dynamic` 페이지는 매 요청마다 서버에서 그때그때 렌더링되어 문제가 드러나지 않았지만, ISR(정적 생성)로 바뀌면서 Next.js가 빌드 타임에 페이지를 미리 렌더링하려고 시도함. 이 과정에서 `Header`, `SubTabBar` 컴포넌트가 최상위에서 바로 `useSearchParams()`를 사용하고 있어 정적 생성 경계 조건을 위반 — 같은 코드라도 렌더링 전략이 바뀌면 새로운 제약 조건이 생긴다는 것을 확인

**해결** 실제 로직을 담은 부분을 내부 컴포넌트로 분리하고 `<Suspense fallback={...}>`으로 감싸 export하는 패턴으로 리팩터링. 로컬에서 `next build`를 직접 돌려 재현·검증한 뒤 배포 (참고: `next dev`와 `next build`를 동시에 실행하면 `.next` 캐시가 꼬여 잘못된 에러가 나므로, 빌드 검증 전 dev 서버를 반드시 종료)

---

### 06. CDN에서 불러오던 폰트 하나가 페이지 전체 전송량의 대부분을 차지하고 있었음

**발견** 브라우저 개발자도구 Network 탭에서 서버 응답(TTFB)은 250ms로 빠른데, "콘텐츠 다운로드"에만 2초 이상 걸리는 걸 확인. 전체 3.3MB 전송량 중 대부분이 폰트였음

**원인** `globals.css` 맨 위에서 Pretendard 폰트 전체 굵기(Light/Regular/Bold/ExtraBold)를 jsdelivr CDN에서 `@import`로 불러오고 있었음 — 서브셋(글자 잘라내기) 처리가 안 된 원본이라 굵기 하나당 750~800KB, 합쳐서 3MB. `@import`는 렌더 블로킹이라 CSS 파싱 자체가 이 다운로드가 끝날 때까지 지연됨

**해결** `pretendard` npm 패키지의 가변 폰트(Variable Font, 전체 굵기를 파일 하나로 커버)를 `next/font/local`로 self-host. CDN 왕복이 사라지고, Next.js가 자동으로 preload + `font-display: swap` 처리해줘서 폰트 로딩이 렌더링을 막지 않게 됨

|                                   | 요청 수 | 용량 |
| --------------------------------- | ------- | ---- |
| CDN `@import` (굵기별 4개)        | 4개     | ~3MB |
| `next/font/local` (가변 폰트 1개) | 1개     | ~2MB |

---

### 07. 상품 상세 페이지가 캐싱을 적용해도 다른 페이지보다 눈에 띄게 느렸습니다

**발견** 리뷰/매거진/패키지 페이지는 캐시가 워밍되면 1초 이내로 빨라지는데, 상품 상세 페이지(`/product/[id]`)만 캐시 워밍 후에도 2초 안팎으로 남아있음

**원인** 코드를 다시 보니 같은 상품 데이터를 **두 번 따로 조회**하고 있었음 — `generateMetadata()`(`<title>` 태그 생성용)와 페이지 컴포넌트가 각자 `prisma.product.findUnique(...)`를 호출. Next.js는 `fetch()`는 자동으로 중복 제거해주지만 Prisma 호출은 그 대상이 아님. 여기에 더해, `product`와 `directorLinks`(작가·패키지·파트너 조회) 쿼리가 서로 값을 필요로 하는 관계가 아닌데도 순서대로(`await` → `await`) 실행되고 있었음

**해결** React의 `cache()`로 `product` 조회 함수를 감싸서 한 요청 안에서는 실제 쿼리가 한 번만 나가도록 통합. `product`와 `directorLinks` 조회는 `Promise.all`로 동시에 실행하도록 변경

|         | 응답시간(캐시 워밍 상태) |
| ------- | ------------------------ |
| 개선 전 | 2.02s                    |
| 개선 후 | 1.55s (약 22%↓)          |

---

### 08. 한국에서 접속해도 응답이 느려서 봤더니, 서버리스 함수가 미국에서 실행되고 있었음

**발견** Neon DB(싱가폴 리전)와의 거리 때문이라고 생각했는데, 한국에서 실측해도 여전히 느려서 응답 헤더를 확인해봄

**원인** `x-vercel-id: icn1::iad1::...` — 요청이 인천(`icn1`) 엣지까지는 빠르게 도달하지만, 실제 서버리스 함수 실행은 미국 동부(`iad1`, 워싱턴 DC)로 넘어가서 처리되고 있었음. "함수 ↔ DB(싱가폴)" 거리보다 "사용자(한국) ↔ 함수(미국)" 거리가 더 큰 병목이었음

**해결** `vercel.json`에 `{ "regions": ["icn1"] }`를 추가해 서버리스 함수 실행 리전을 서울/인천으로 고정. 자체 서버 전환 전까지, 한국 기반 트래픽(현재 테스트/데모 대상)에 대해 미국 왕복 구간을 제거. 배포 후 `x-vercel-id`가 `icn1::icn1::...`로 바뀐 것을 확인함

|         | `x-vercel-id`                 | 상품 상세 응답시간      |
| ------- | ----------------------------- | ----------------------- |
| 개선 전 | `icn1::iad1::...` (미국 경유) | 1.55s                   |
| 개선 후 | `icn1::icn1::...` (서울 고정) | 0.44~0.49s (약 70~90%↓) |

---

### 09. 이미지 많은 페이지가 배포 여부와 무관하게, 캐시가 비어있을 때마다 심하게 느렸음

**발견** `/packages`처럼 썸네일이 많은 페이지에서 특정 사진들이 브라우저 Network 탭 기준 10~50초씩 걸리는 게 확인됨. Cloudflare 캐시(`cf-cache-status`)가 이미 `HIT`인 요청도 있었지만, 아직 한 번도 요청된 적 없는(콜드) 이미지들은 여전히 느렸음

**원인** 두 겹의 문제가 겹쳐 있었음

1. 업로드 시점 압축 기본값이 `3840px / webp quality 92`로, 리사이즈용 "썸네일"까지 전부 이 값으로 저장되고 있어서 원본 파일 하나가 1MB를 넘는 경우가 흔했음
2. `next/image`가 요청마다 그 원본을 다시 서버(RAM 1.9GB짜리 소형 VPS)에서 sharp로 리사이즈하는데, 캐시가 없는 상태에서 여러 장이 동시에 요청되면 서버가 감당을 못 해 요청들이 줄줄이 밀림

**시도 1 — 캐시 수명 늘리기** `next.config.ts`의 `images.minimumCacheTTL`을 60초(기본값)에서 1달로 늘리고, Cloudflare Cache Rule로 `/_next/image*` 요청을 엣지에서 장기간(원본 헤더 무시하고 강제로) 캐싱하도록 설정. R2 업로드 파일명은 매 업로드마다 랜덤 UUID가 붙어 URL이 재사용되지 않으므로, 오래 캐싱해도 옛 이미지가 보이는 문제는 생기지 않음 — 캐시가 워밍된 이후로는 확실히 빨라졌지만, "처음 요청되는" 콜드 케이스 자체는 여전히 느렸음

**시도 2 — 서버 리사이즈 완전히 생략** `images.unoptimized: true`로 Next의 서버 리사이즈 단계 자체를 건너뛰고 R2 원본 URL을 그대로 서빙 — 콜드 캐시 문제는 사라졌지만, 반응형 srcset이 없어지면서 페이지 전체 전송량이 늘어 체감상 더 무거워짐 (Lighthouse 성능 점수도 하락)

**최종 해결** `unoptimized`는 되돌리고(Next 서버 리사이즈 유지), 대신 **업로드 시점 압축 기본값을 낮춤**(`3840px/q92` → `1920px/q78`) — 화면을 꽉 채우는 히어로 대표 사진만 파일명 접두사(`hero*`)로 구분해서 기존 고화질을 유지. 이미 업로드된 기존 이미지들은 `scripts/recompress-images.ts`로 새 압축값으로 일괄 재처리(새 URL로 재업로드 후 DB 갱신, 옛 R2 오브젝트 삭제) — 원본 대비 용량이 75~80%가량 줄어듦(예: 629KB→147KB)

|                    | 업로드 압축                | 서버 리사이즈     | 콜드 캐시 응답      |
| ------------------ | -------------------------- | ----------------- | ------------------- |
| 개선 전            | 3840px / q92 (파일당 ~1MB) | 매번 재계산       | 요청당 10~50s       |
| `unoptimized` 시도 | 원본 그대로                | 생략              | 빠름 (대신 전송량↑) |
| 최종               | 1920px / q78 (히어로 제외) | 캐시 워밍 후 빠름 | 정상 범위           |

**후속 — 사전 캐시 웜업과 그 한계**

배포 직후 첫 방문자가 콜드 캐시를 겪지 않도록 `scripts/warm-image-cache.ts`를 만들어, DB의 모든 이미지 URL을 미리 `/_next/image`로 요청해 Next 서버 캐시 + Cloudflare 엣지 캐시를 데워두는 방식을 시도함. 과정에서 캐시가 URL의 **모든 구성요소**(width, quality, 그리고 `Vary: Accept` 헤더로 인한 클라이언트 Accept 값)에 따라 각각 별도로 나뉜다는 걸 확인:

- 처음엔 대표 폭 2개(640/1920)만 워밍했는데, 실제 브라우저는 `next/image`의 기본 `deviceSizes`(640~3840px, 총 8개) 중 화면 크기·해상도(DPR)에 따라 다른 값을 요청하고 있어서 나머지 폭은 여전히 콜드였음 → 8개 폭 전체로 확장
- `/_next/image` 응답에 `Vary: Accept`가 붙어있어, 클라이언트가 보내는 Accept 헤더 값에 따라서도 캐시가 나뉠 수 있음(확인해보니 Cloudflare가 실제로는 이 값을 크게 구분하지 않는 것으로 보였지만, 방어적으로 브라우저와 동일한 Accept 헤더를 스크립트에 명시)
- 그럼에도 특정 기기(아이폰 등)에서 여전히 느린 사례가 있었음 — Cloudflare는 전 세계 수백 개의 엣지(PoP)에 분산되어 있어서, 워밍 요청이 도달한 엣지와 실제 사용자가 접속하는 엣지가 다르면 그 엣지 입장에서는 여전히 첫 요청(콜드)이 됨

**결론** "모든 기기 × 브라우저 × 화면크기 × 엣지 조합"을 사전에 전부 커버하는 건 사실상 불가능하다고 판단하고, 웜업 스크립트는 "완전한 보장"이 아니라 "콜드 미스 확률을 낮추는 보조 수단"으로 목표를 재설정함. 애초에 잡으려던 문제(요청당 10~50초의 극단적 지연)는 업로드 압축 최적화로 이미 해결된 상태라, 남은 건 어떤 서비스에나 있는 정상 범위의 가끔 있는 콜드 미스

---

</details>
